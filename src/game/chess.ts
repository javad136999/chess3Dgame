import{Chess}from'chess.js';export type Mode='offline'|'online';export const newGame=()=>new Chess();export const legalMoves=(game:Chess,square:string)=>game.moves({square:square as any,verbose:true});export const pieceSymbol=(p:any)=>p?.type?.toUpperCase()||'';export const isGameOver=(g:Chess)=>g.isGameOver();export const statusText=(g:Chess)=>g.isCheckmate()?'Checkmate':g.isDraw()?'Draw':g.isCheck()?'Check':'Your turn';