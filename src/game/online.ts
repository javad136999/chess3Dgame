import { supabase } from '../lib/supabase';
export async function createRoom(userId:string){ if(!supabase) throw Error('Supabase env is not configured'); const {data,error}=await supabase.from('chess_rooms').insert({white_id:userId,fen:'startpos',turn:'w',status:'waiting'}).select().single(); if(error) throw error; return data; }
export async function joinRoom(roomId:string,userId:string){ if(!supabase) throw Error('Supabase env is not configured'); const {data,error}=await supabase.from('chess_rooms').update({black_id:userId,status:'playing'}).eq('id',roomId).is('black_id',null).select().single(); if(error) throw error; return data; }
export function subscribeRoom(roomId:string,onChange:(room:any)=>void){ if(!supabase) return ()=>{}; const channel=supabase.channel('chess-room-'+roomId).on('postgres_changes',{event:'*',schema:'public',table:'chess_rooms',filter:'id=eq.'+roomId},payload=>onChange(payload.new)).subscribe(); return ()=>{supabase.removeChannel(channel)}; }
export async function savePosition(roomId:string,fen:string,turn:string){ if(!supabase) throw Error('Supabase env is not configured'); return supabase.from('chess_rooms').update({fen,turn,updated_at:new Date().toISOString()}).eq('id',roomId); }
